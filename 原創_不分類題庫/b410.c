#include <stdio.h>

int main(int argc, const char * argv[]) {
    int n, i;
    while (scanf("%d",&n) != EOF){
        for(i = 0 ; i < n ; i ++){
            printf ("HELLO!\n");
        }
    }
    return 0;
}
